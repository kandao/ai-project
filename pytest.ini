[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
pythonpath = tests
markers =
    e2e: End-to-end tests requiring full infrastructure (Postgres, Redis, Kafka)
    slow: Tests that take longer to run (polling, timeouts)
